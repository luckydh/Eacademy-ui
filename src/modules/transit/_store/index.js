const state = {}
const getters = {}
const mutations = {}
const actions = {
	login: async (context, payload) => {
		// let { data } = Axios.get()
		// context.commit('KEY', value)
	}
}

export default{
    state, getters, mutations, actions
}